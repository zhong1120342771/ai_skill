# 代码生成要求和说明

> 来源：飞书文档 https://zhuanspirit.feishu.cn/wiki/ESn4wSuAwiQ08jkht3UcKuFynsb

## 一、取数平台连接

- 星河平台连接 skill(附件 token:`V0zobvxjooB97BxwOFacrXOanDd`,mime: text/markdown)
- **凭证一律走环境变量,不要写到任何脚本/SQL/SKILL 里**:
  - 服务名 → `$XINGHE_CLIENT_USER`
  - 星河密码 → `$XINGHE_CLIENT_SECRET`
  - 星河 accessKey → `$XINGHE_ACCESS_KEY`
  - One-Service 兜底 → `$ONESERVICE_OA` / `$ONESERVICE_ACCESS_KEY`
- 拿到 skill 后第一步:在 `~/.zshrc` 里 export 上述变量,`source ~/.zshrc` 让取数脚本能读到。**不要把同事的凭证抄过来**——服务名按人维度,要用自己的星河账号。

## 二、取数说明

**步骤1**：每日圈定人群（抽样 1 万用户），取出人群包和对应的人群标签（用户来源和用户资产分层），这个人群包 `data1` 为后续所有分析的基础。

- SQL：[Scripts/用户抽样&用户标签sql.sql](Scripts/用户抽样&用户标签sql.sql)

**步骤2**：`inner join` data1 中的用户 token，取出该批用户首页全量的点曝数据（data1 中的用户标签字段也要获取），得到曝光数据集 `data2`、点击数据集 `data3`；取出全量页面的曝光和用户启动和退出 app 的明细数据（data1 中的用户标签字段也要获取），得到数据集 `data4`。

- SQL：
  - [Scripts/首页曝光事件sql.sql](Scripts/首页曝光事件sql.sql)
  - [Scripts/首页点击事件sql.sql](Scripts/首页点击事件sql.sql)
  - [Scripts/页面访问&app停留时长sql.sql](Scripts/页面访问&app停留时长sql.sql)

**步骤3**：将 data2、data3、data4 中的页面 id（`actiontype`）和区域 id（`section_id`）进行中文名称的映射，映射信息来自两张维表，维表表结构见下方 SQL，需自行写 SQL 连接获取。映射成功后得到 `data2-2`、`data3-2`、`data4-2`。

- 维表结构：
  - [Scripts/页面id和页面名称的映射.sql](Scripts/页面id和页面名称的映射.sql)
  - [Scripts/区域id和区域名称的映射.sql](Scripts/区域id和区域名称的映射.sql)
